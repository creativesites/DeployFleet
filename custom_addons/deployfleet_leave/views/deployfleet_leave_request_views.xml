<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_deployfleet_leave_request_form" model="ir.ui.view">
        <field name="name">deployfleet.leave.request.form</field>
        <field name="model">deployfleet.leave.request</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <button name="action_submit" type="object" string="Submit"
                            invisible="state != 'draft'" class="btn-primary"/>
                    <button name="action_approve" type="object" string="Approve"
                            invisible="state != 'submitted'" class="btn-primary"/>
                    <button name="action_reject" type="object" string="Reject"
                            invisible="state != 'submitted'"/>
                    <button name="action_cancel" type="object" string="Cancel"
                            invisible="state in ('cancelled', 'rejected')"/>
                    <field name="state" widget="statusbar" statusbar_visible="draft,submitted,approved"/>
                </header>
                <sheet>
                    <group>
                        <group>
                            <field name="employee_id"/>
                            <field name="leave_type_id"/>
                        </group>
                        <group>
                            <field name="date_from"/>
                            <field name="date_to"/>
                            <field name="number_of_days"/>
                        </group>
                    </group>
                    <field name="reason" placeholder="Reason..."/>
                </sheet>
            </form>
        </field>
    </record>

    <record id="view_deployfleet_leave_request_list" model="ir.ui.view">
        <field name="name">deployfleet.leave.request.list</field>
        <field name="model">deployfleet.leave.request</field>
        <field name="arch" type="xml">
            <list>
                <field name="employee_id"/>
                <field name="leave_type_id"/>
                <field name="date_from"/>
                <field name="date_to"/>
                <field name="number_of_days"/>
                <field name="state" widget="badge"
                       decoration-info="state in ('draft', 'submitted')"
                       decoration-success="state == 'approved'"
                       decoration-danger="state == 'rejected'"
                       decoration-muted="state == 'cancelled'"/>
            </list>
        </field>
    </record>

    <record id="action_deployfleet_leave_request" model="ir.actions.act_window">
        <field name="name">Leave Requests</field>
        <field name="res_model">deployfleet.leave.request</field>
        <field name="view_mode">list,form</field>
    </record>

    <menuitem id="menu_deployfleet_leave_request"
              name="Leave"
              parent="deployfleet_core.menu_deployfleet_root"
              action="action_deployfleet_leave_request"
              groups="deployfleet_security.group_deployfleet_dispatcher"
              sequence="58"/>
</odoo>
