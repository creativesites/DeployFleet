# Copy to .env and fill in local values. Never commit the real .env file.

# PostgreSQL
POSTGRES_DB=deployfleet
POSTGRES_USER=deployfleet
POSTGRES_PASSWORD=changeme_local_dev_only

# Odoo
ODOO_HTTP_PORT=8169
ODOO_LONGPOLLING_PORT=8172

# deployfleet_ai_core — never commit a real key here, this is a template only
DEPLOYFLEET_AI_DEEPSEEK_API_KEY=
DEPLOYFLEET_AI_OPENAI_API_KEY=
DEPLOYFLEET_AI_CLAUDE_API_KEY=
