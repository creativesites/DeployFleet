# CLAUDE.md — DeployFleet Operating Guide

This is the permanent operating guide for every development session on DeployFleet. Read it before touching code. It is a living document — update it whenever a convention, architecture decision, or workflow rule changes, in the same session that changes it.

**Current phase: Experience Architecture implementation.** All 43 backend modules (Phase 0–5 plus the demo-data module) are functionally complete, tested, and deployed. Frontend work now proceeds against [docs/architecture/16-experience-architecture.md](docs/architecture/16-experience-architecture.md) through [19-animation-guidelines.md](docs/architecture/19-animation-guidelines.md)'s phased rollout, starting with the shared `deployfleet_ui` design-system/component-library module (Phase A). The real trucking-operator domain-model validation flagged in [docs/architecture/06-risks-and-recommendations.md](docs/architecture/06-risks-and-recommendations.md) and [07-domain-model-erd.md](docs/architecture/07-domain-model-erd.md) remains open and should still happen — it is not a gate on frontend work, which builds on the domain model as it stands today.

---

## 1. What DeployFleet is

DeployFleet is an AI-powered logistics operating system for transportation and logistics companies, built by forking and transforming DeployGuard (`creativesites/DogFrce-Security-Services-Custom-Odoo-Modules`), a mature Odoo 19 Community platform originally built for private security companies. Initial market: local trucking companies in Zambia, expanding to regional Southern African logistics operators.

**This is not a generic ERP customization.** It is a commercial, multi-tenant-capable product with its own module namespace, its own AI architecture, its own mobile apps, and its own marketing site — DeployGuard is the donor codebase, not a dependency DeployFleet stays coupled to.

### Governing philosophy

> Reuse everything that is generic. Refactor everything that is industry-specific. Remove only what cannot reasonably be reused.

Concretely:

- **Reuse as-is or with light rework:** authentication, Odoo ACL/security-group patterns, workflow/state-machine conventions, the notification engine, reporting/dashboard shells, the mobile REST API pattern, the AI provider-abstraction framework, billing pipeline machinery, payroll/localization engine, documentation tooling, enterprise UI conventions.
- **Redesign the domain vocabulary, not just rename it:** guard → driver, post/site → depot/terminal, shift → trip, roster → dispatch, patrol → route, security incident → breakdown/driver-performance incident. See [docs/architecture/01-module-audit.md](docs/architecture/01-module-audit.md) for the full module-by-module classification and [docs/architecture/07-domain-model-erd.md](docs/architecture/07-domain-model-erd.md) for why this is a genuine remodel (`customer → shipment → route → dispatch → trip → delivery`), not a find-and-replace.
- **Never fork:** `security_dogforce_data` or anything containing DogForce's real production/client data. See [docs/architecture/06-risks-and-recommendations.md](docs/architecture/06-risks-and-recommendations.md) risk #1 — this is a hard rule, not a style preference.

---

## 2. Architecture documentation — read before writing code

The full Phase 1 architecture analysis lives in `docs/architecture/`, indexed at [docs/architecture/README.md](docs/architecture/README.md). It is the source of truth; this file organizes and summarizes it, it does not replace it.

| Doc | Covers |
|---|---|
| [01-module-audit.md](docs/architecture/01-module-audit.md) | Every DeployGuard module, classified keep/rename/refactor/split/remove |
| [02-reuse-strategy.md](docs/architecture/02-reuse-strategy.md) | Reusable business logic (incl. the event bus), UI, security groups, reports, APIs, mobile code |
| [03-refactoring-roadmap.md](docs/architecture/03-refactoring-roadmap.md) | Naming/model-identity strategy (the `deployfleet.*` namespace, vehicle delegation pattern) and technical debt triage |
| [04-module-structure.md](docs/architecture/04-module-structure.md) | The ~50-module target structure, dependency graph, MVP-12 first release |
| [05-implementation-roadmap.md](docs/architecture/05-implementation-roadmap.md) | The 5-phase build sequence (Operational Foundation → Cost Control → Compliance → Customer Platform → Intelligence), Phase 0 prerequisites |
| [06-risks-and-recommendations.md](docs/architecture/06-risks-and-recommendations.md) | Hard risks, moderate risks, open questions requiring explicit sign-off |
| [07-domain-model-erd.md](docs/architecture/07-domain-model-erd.md) | Entity model, relationships, key workflows — the schema Phase 1 builds against |
| [08-ai-architecture.md](docs/architecture/08-ai-architecture.md) | AI provider strategy, cost/permission architecture, agent catalog, mandatory action-approval pipeline |
| [16-experience-architecture.md](docs/architecture/16-experience-architecture.md) | The full UX/frontend strategy — gates all future frontend work the same way this doc gates AI work. Written after functional completion of all 43 modules; audits DeployFleet's current 100%-stock-Odoo UI, grounds DeployGuard OWL-pattern reuse in an actual source read, sets a deliberate Tesla/Stripe/Linear visual personality distinct from DeployGuard's military-ops register, and gives area-by-area redesign direction (Dispatch Board and the Copilot Console are the flagged top priorities) |
| [17-design-system.md](docs/architecture/17-design-system.md) | The token-level design system beneath doc 16 — color (incl. the AI-content violet convention, dark mode), spacing, typography, elevation/glass, iconography, accessibility |
| [18-component-library.md](docs/architecture/18-component-library.md) | The `deployfleet_ui` OWL component catalog — atoms, composites, layout shells, and the signature "wow" widget set, each with its concrete DeployFleet-model data source |
| [19-animation-guidelines.md](docs/architecture/19-animation-guidelines.md) | The motion system — timing/easing scale and a per-interaction spec table, so animation reads as one language, not five guesses |
| [20-experience-implementation-strategy.md](docs/architecture/20-experience-implementation-strategy.md) | **Governing rollout strategy — read this before starting frontend work on any domain.** The product philosophy ("this shouldn't feel like Odoo"), the tightened New UI Rule (every operational entity gets a custom view, including simple master data), and the domain-completeness-first rollout principle that supersedes doc 16 §11's horizontal phased rollout — finish one domain entirely (every stock screen replaced, polished, click-tested) before starting the next |

Root-level summary docs (`ARCHITECTURE.md`, `MODULE_STRUCTURE.md`, `AI_ARCHITECTURE.md`, `DATABASE_DESIGN.md`, `MOBILE_ARCHITECTURE.md`, `DEPLOYMENT.md`, `CHANGELOG.md`) exist as short entry points that point into the detailed docs above — keep them short; put real analysis in `docs/architecture/`, not duplicated at root.

**Before making any change that touches module structure, naming, or the domain model, check whether it's already decided in these docs.** If it contradicts something decided there, raise it explicitly rather than silently diverging — these documents are the record of decisions already made, including *why*, and silent drift between them and the code is exactly the kind of debt this project is trying not to inherit from DeployGuard.

---

## 3. Naming conventions (binding, not a suggestion)

| Layer | Convention | Example |
|---|---|---|
| Python package / addon folder | `deployfleet_<domain>` | `deployfleet_dispatch`, `deployfleet_ai_core` |
| Odoo technical model name (net-new) | `deployfleet.<entity>` | `deployfleet.trip`, `deployfleet.shipment` |
| Odoo technical model name (delegates Odoo core, e.g. vehicle) | `deployfleet.<entity>` with `_inherits` | `deployfleet.vehicle` (`_inherits` `fleet.vehicle`) |
| Odoo technical model name (classical extension, e.g. employee) | `_inherit` the core model directly | `_inherit = "hr.employee"` for driver fields |
| Security group `xml_id` | `group_deployfleet_<role>` | `group_deployfleet_driver`, `group_deployfleet_dispatcher` |

**Never use a bare `fleet_` or `fleet.` prefix for anything DeployFleet owns.** Odoo ships a native Fleet app with `fleet.vehicle` and related models — see [03-refactoring-roadmap.md](docs/architecture/03-refactoring-roadmap.md) §A. `deployfleet.vehicle` uses delegation inheritance (`_inherits`) over `fleet.vehicle`, not a standalone reinvention and not a plain field-bolt-on `_inherit` — this is a resolved decision, not an open one; don't re-litigate it per module.

---

## 4. AI architecture — the short version

Full design in [08-ai-architecture.md](docs/architecture/08-ai-architecture.md). The governing principle, binding on every AI feature:

> DeployFleet is an operations platform where AI assists humans, automates repetitive work, detects problems early, and provides intelligence — but every AI capability is modular, permission-controlled, measurable, and optional.

Non-negotiable rules, not aspirations:

- **Every AI capability is independently switchable** — global on/off, plus per-agent/per-feature toggles. No AI feature ships without a way to turn it off.
- **No business module talks to an AI provider directly.** Every call goes through `deployfleet_ai_core`'s router. Provider list: DeepSeek (default), OpenAI, Claude, with a cheap/reasoning model tier per feature. Adding or swapping a provider must never require touching a business module.
- **Any AI-initiated write is gated**: suggestion → permission check → human approval → execute → audit log. There is no "auto-execute" path in v1. This is flagged as hard risk #7 in [06-risks-and-recommendations.md](docs/architecture/06-risks-and-recommendations.md) — verify it in code review, don't just trust the design doc.
- **Every AI call is logged and cost-tracked** (`deployfleet.ai.usage`) — tokens, cost, cache hits, per company. Don't ship a feature that calls a provider without this.
- **Data-sensitivity matters**: payroll/financial data needs a per-feature opt-out from external AI providers, not just a global switch — see [06-risks-and-recommendations.md](docs/architecture/06-risks-and-recommendations.md) risk #8.

Before writing new AI code, check [08-ai-architecture.md](docs/architecture/08-ai-architecture.md) §0 — a surprising amount of this (provider routing, caching, per-feature toggles, usage logging) is already ~70% built in the DeployGuard source (`security_ai_engine`) and needs porting/extending, not building from zero.

---

## 5. UI/UX standards

**The authoritative elaboration of this section is now a five-document set: [16-experience-architecture.md](docs/architecture/16-experience-architecture.md) (visual strategy), [17-design-system.md](docs/architecture/17-design-system.md) (tokens), [18-component-library.md](docs/architecture/18-component-library.md) (the `deployfleet_ui` component catalog), [19-animation-guidelines.md](docs/architecture/19-animation-guidelines.md) (motion), and [20-experience-implementation-strategy.md](docs/architecture/20-experience-implementation-strategy.md) (rollout strategy — read this one first)** — check any dashboard/board/workspace design or build against all five before starting, the same discipline §4 already requires against the AI architecture doc. The bullets below are the short-form summary; the real analysis lives there.

- **Mobile-first.** Primary users are drivers, dispatchers, and fleet managers/supervisors working from phones and tablets, not desks. Design and test for phone and tablet viewports first, desktop second.
- **Modern glassmorphic, enterprise-clean aesthetic — not default Odoo chrome.** DeployFleet should not feel like an Odoo ERP; it should feel like a modern logistics operating system that happens to be built on Odoo (doc 20 §1) — this is a product-philosophy statement, not just a visual-taste one, because how the product feels in the first five minutes of a demo is treated as a direct driver of win rate.
- **Custom views are the default for every operational domain entity, not the exception.** Doc 20 §2 tightens the older "use judgment" framing: as much as reasonably possible, every operational area gets custom views instead of stock Odoo list/form/kanban pages — including simple master data (e.g. Vehicle Types), because even a nine-field config screen reads as "ERP" the moment a user sees it mid-demo. Default Odoo pages become implementation details a user is not meant to routinely encounter. The remaining exception is pure admin/system configuration (AI provider config, notification rules, the event bus, mobile device registrations, licenses) — a user never opens these to do their job, only an implementer during setup.
- **Domains are built one at a time, to completion, not one screen-type at a time across every domain.** Doc 20 §5's domain-completeness-first rollout: audit → design (collaboratively with the user) → replace every stock screen in that domain → build supporting workspaces → polish → click-test → only then move to the next domain. No partially redesigned domains left behind while attention moves elsewhere.

### Mobile applications — three, not one

Per [04-module-structure.md](docs/architecture/04-module-structure.md), mobile splits by role rather than staying one app with three logins:

| App | Primary users | Core workflows |
|---|---|---|
| Driver | Drivers | Assigned trips, navigation, pre/post-trip inspections, proof of delivery, breakdown reporting, document access, dispatcher communication |
| Dispatcher | Dispatchers, fleet managers | Live operations view, trip monitoring, alerts, approvals |
| Customer | Shippers/customers | Shipment tracking, delivery status, documents |

Full detail: [MOBILE_ARCHITECTURE.md](MOBILE_ARCHITECTURE.md).

### Marketing website — separate from Odoo entirely

DeployFleet's marketing site is **not** an Odoo module and does not live in this repository's Odoo addon tree. Stack: **Next.js, deployed on Vercel.** Scope: SaaS landing page, product/feature pages, documentation, demo-request and pricing/onboarding flows. Treat it as an independent project with its own repo or a clearly separated top-level directory — never let it become a dependency of the Odoo backend or vice versa.

---

## 6. Deployment safety rules

Full detail: [DEPLOYMENT.md](DEPLOYMENT.md). The short version, because getting this wrong is a production incident on someone else's live system:

- A demo server exists and **already hosts a live production Odoo instance and a staging instance that must never be disrupted.** DeployFleet gets its own database, its own filestore, its own Docker stack, and its own port allocation — full stop.
- **Before any deployment action against that server**, inventory what's already running: containers, active ports, databases, volumes, reverse-proxy config. This inventory has not been done yet as of this writing — it's a required first step of actual deployment work, not something to skip because "it's probably fine."
- **Never commit credentials, ever, to any file in this repository** — not in CLAUDE.md, not in a `.env.example` with a real value slipped in, not in a script "just for now." If a credential is ever pasted into a chat or a doc by mistake, treat it as compromised and get it rotated — don't just delete the file and move on.

---

## 7. Git commit standards

Conventional-commits style, required for every commit:

```
type(scope): description
```

Examples: `feat(dispatch): add trip assignment workflow`, `feat(ai): add DeepSeek provider adapter`, `refactor(vehicle): redesign vehicle lifecycle model`, `docs(architecture): update module structure`, `fix(mobile): resolve offline sync issue`.

**Never** commit with messages like `update`, `changes`, `fix stuff`, `testing`. If you can't summarize the change in a `type(scope): description` line, the change is probably too large or too unfocused for one commit.

---

## 8. Development workflow — every session

1. Read this file (`CLAUDE.md`) end to end if it's been more than a few sessions since the last read.
2. Review `docs/architecture/` for anything relevant to the task at hand — don't assume you remember the current decisions.
3. Check `git status` and recent log before making changes.
4. Understand the existing module(s) you're touching before extending them.
5. Make minimal, focused changes — this project explicitly avoids "while I'm in here" scope creep (see the source DeployGuard's own guidance against unnecessary rewrites, which DeployFleet inherits as a value, not just a codebase).
6. Update documentation in the same session as the code change it describes, not "later."
7. Run tests before considering a change done.
8. Commit with a conventional-commits message. Clean `git status` at session end — no dangling untracked files that should have been committed or ignored.

---

## 9. Quality bar

DeployFleet is a commercial SaaS product, not a one-off customer customization. Every module should be held to:

- Clean, maintainable architecture — the module boundaries in [04-module-structure.md](docs/architecture/04-module-structure.md) exist to keep this true, not just to look tidy on a diagram.
- Multi-company and multi-country support as a design constraint from the start, not retrofitted — see the payroll/country-pack decoupling fix in [03-refactoring-roadmap.md](docs/architecture/03-refactoring-roadmap.md), which exists specifically because the source codebase got this wrong once already.
- Secure, auditable permissions — both Odoo's own ACL/record-rule model and, where AI is involved, the AI-specific permission layer in [08-ai-architecture.md](docs/architecture/08-ai-architecture.md) §9.
- Documented APIs (mobile REST endpoints, AI action contracts).
- Automated tests — set the coverage bar from the first module, don't defer it the way the source codebase's `KNOWN_ISSUES.md` shows DeployGuard did for mobile controller tests.
- Production deployment readiness — CI green, no hardcoded credentials, no dependence on manual server-side steps that aren't documented in `DEPLOYMENT.md`.

---

## 10. Current status and next step

**Done:** the Phase 1 architecture analysis (`docs/architecture/01` through `15`), this operating guide, the supporting root-level docs listed in §2, and formal sign-off resolving 7 of the 8 hard risks in [06-risks-and-recommendations.md](docs/architecture/06-risks-and-recommendations.md). **Phase 0** (`deployfleet_core`, `deployfleet_security`, `deployfleet_event_bus`, `deployfleet_ai_core`), **Phase 1 — Operational Foundation** (`deployfleet_driver`, `deployfleet_vehicle`, `deployfleet_customer`, `deployfleet_route`, `deployfleet_dispatch`, `deployfleet_trip`, `deployfleet_delivery`), **Phase 2 — Cost Control** (`deployfleet_freight_calculator`, `deployfleet_load_expense`, `deployfleet_driver_advance`, `deployfleet_fuel`, `deployfleet_parts`, `deployfleet_tyres`, `deployfleet_workshop`, `deployfleet_maintenance`, `deployfleet_assets`), **Phase 3 — Compliance** (`deployfleet_compliance`, `deployfleet_vehicle_compliance`, `deployfleet_dispatch_compliance`, `deployfleet_insurance`, `deployfleet_payroll`, `deployfleet_l10n_zm`, `deployfleet_leave`, `deployfleet_loans`, `deployfleet_driver_performance`), **Phase 4 — Customer Platform** (`deployfleet_billing`, `deployfleet_accounting`, `deployfleet_zra`, `deployfleet_client_reports`, `deployfleet_customer_portal`, `deployfleet_notifications`, `deployfleet_mobile_dispatcher`, `deployfleet_mobile_customer`, `deployfleet_mobile_push_bridge`, `deployfleet_ai_permissions`, `deployfleet_ai_actions`, `deployfleet_ai_whatsapp`), and **Phase 5 — Intelligence** (`deployfleet_ai_agents`: the six-agent catalog plus predictive maintenance, statistical fuel-anomaly detection, historically-weighted dispatch scoring, and financial forecasting) are all implemented, tested, lint-clean, and committed — 42 modules total, plus a 43rd demo/tooling module, `deployfleet_demo_zm`, a `post_init_hook`-driven full-system demo dataset for a fictional Zambian trucking company, "CreativeSites Logistics" — company/currency setup, depots, customers/contracts, vehicles (incl. one breakdown, one expired-insurance), drivers, routes, insurance/compliance documents, maintenance/workshop/parts/tyres/assets, fuel logs with a deliberate anomaly, shipments run through the full dispatch → trip → delivery → invoice pipeline (incl. one via the compliance-override path), HR extras (leave/advances/loans/performance/payslips), AI action requests across pipeline states, and pre-populated Phase 5 predictions — built via each model's normal `create()`/`action_*()` methods, never calling the AI provider router, leaving ZRA config untouched, and registering no push device tokens so install makes no outbound network calls.

Phase 4/5 notes worth carrying forward, not re-discovering later: `deployfleet_zra`'s endpoint paths/field names come from a community Postman collection, not ZRA's own spec PDF (which 503'd while building it) — verify against ZRA's actual sandbox before any real submission, and get someone who knows ZRA's VAT category rules to review the default VAT category code. `deployfleet_ai_whatsapp`'s webhook does not yet verify Meta's `X-Hub-Signature-256` signature — not hardened against spoofed requests until that's closed. `deployfleet_ai_agents`' predictive models use real closed-form statistics (linear regression, z-scores) calibrated on whatever seed/demo data exists, not the real pilot-customer history its own Phase 5 exit criteria call for — treat current outputs as illustrative, not validated. All three are flagged in their own module READMEs, not just here.

**A live demo is deployed and running** on the shared demo server at `http://199.192.23.46:4169/`, alongside — not disrupting — that server's pre-existing production and staging Odoo instances, per the isolated stack in [DEPLOYMENT.md](DEPLOYMENT.md). All 43 modules — Phase 0 through Phase 5 plus `deployfleet_demo_zm` — are installed on the demo database, with the full "CreativeSites Logistics" demo dataset generated. DeepSeek is configured as the active AI provider via the Odoo backend (`deployfleet.ai.config`), not via any file in this repository.

The demo server runs an Odoo 19 nightly snapshot (`19.0-20260723`), not a stable release — this has already caused several version-drift bugs during install (`res.groups.category_id` removed in favor of `privilege_id`; the base settings view's `div.settings` xpath anchor changed; `product.product`'s `invoice_policy` field turned out to belong to the `sale` module, not core `product`, which is unrelated to version drift but was caught the same way; `res.users.groups_id` renamed to `group_ids` and the corresponding `res.groups.users` renamed to `user_ids`). Pinning to a stable Odoo 19 release once one is cut remains an open recommendation, not yet decided. Also learned the hard way this session: a `<record id="base.user_admin">` in any non-`base` module's data file is silently skipped forever, since that xmlid's `noupdate="1"` is set by `base` itself — use a `<function>` call instead whenever a module needs to write to a record it doesn't own the xmlid for.

**Real, previously-undiscovered gap found while generating demo data, not just a demo issue:** no DeployFleet module ever provisions a chart of accounts for a company. A fresh company (no country fiscal localization installed — `deployfleet_l10n_zm` is payroll-tax rules only, not a CoA) has zero `account.account` records, so `deployfleet_accounting`'s `_create_account_move()` used to fail on literally the first invoice any company ever confirmed (`account_move_line_check_accountable_required_fields`, NULL `account_id`). Given a stopgap in `deployfleet_invoice.py`: `_create_account_move()` now lazily creates one dedicated income account and one dedicated receivable account (idempotent by code) and assigns them before posting, instead of requiring Odoo's full accounting-onboarding wizard to have run first. This unblocks first-invoice posting for any company, demo or real, but it does **not** resolve the actual chart-of-accounts/localization strategy question below — that's still open and shouldn't be treated as closed by this stopgap.

**Still genuinely open, and not to be silently closed just because implementation has progressed well past Phase 0:**

- Hard risk #5: the domain model in [07-domain-model-erd.md](docs/architecture/07-domain-model-erd.md) has still not been validated against a real trucking operator's actual dispatch workflow. Implementation has run ahead of this discovery pass — a known, explicitly-flagged gap, not a silently-dropped one.
- The remaining open questions in [06-risks-and-recommendations.md](docs/architecture/06-risks-and-recommendations.md): Odoo edition (Community vs. Enterprise — this also blocks a clean answer to [15-load-sheet-architecture.md](docs/architecture/15-load-sheet-architecture.md) §9's custom-fields question), confirmed launch-market scope, team size/timeline against the 5-phase roadmap, and the concrete default AI token/cost budget numbers for the MVP release.
- The root SSH password used to deploy the demo was pasted into this project's session history during setup — rotate it regardless of whether the deploy scripts ever actually reached the server with it.
- CI's `mobile-typecheck` job has been failing on every run since Phase 0 for reasons that don't trace to this repo's content (near-instant failures with no runner ever assigned, per GitHub's own job metadata) — flagged, not yet root-caused. The separate, definite `lint` job failure (pylint couldn't resolve `import odoo` with no ignored-modules configured) has been fixed and confirmed locally.

**Experience Architecture implementation has begun.** `deployfleet_ui` (module 44) is a new shared, dependency-free-of-business-logic OWL library — no business module should ever hand-roll a button, card, or badge again, per [18-component-library.md](docs/architecture/18-component-library.md)'s governance rule. This is Phase A / Slice 1 of doc 16 §11's rollout: the design-system tokens from [17-design-system.md](docs/architecture/17-design-system.md) as real SCSS custom properties (color incl. the AI-content violet convention and dark mode, spacing, typography, elevation/glass per Command/Workspace layer) and the motion tokens/keyframes from [19-animation-guidelines.md](docs/architecture/19-animation-guidelines.md), plus a first tranche of foundational OWL components (Button, Card, Avatar, StatusBadge, StatusPill, AiBadge) and one composite (MetricCard) — not doc 18's full catalog yet. A debug-only "UI Component Showcase" client action (`base.group_no_one`) renders every component and variant as a living reference.

**Important caveat, unlike every backend module before it:** this module's JS/OWL/SCSS has been verified for syntax validity only (XML well-formedness, genuine JS syntax checking via `node --check` against a `.mjs` copy — a plain `.js` copy silently no-ops the check when the file starts with an `import` statement, a real gotcha hit and worked around this session) and structural correctness against standard Odoo 17–19 OWL conventions. **None of it has been verified by actually rendering it in a running Odoo web client** — this development environment has no browser or live Odoo instance to load one in, unlike the Python modules, which were all validated by installing on the live demo server and reading the resulting log. The Component Showcase screen exists specifically so a human can do that check on first install. Expect this to be the first module in the project where the install log alone isn't sufficient sign-off — actually opening the showcase screen and looking at it matters here in a way it didn't for the backend-only modules.

**`deployfleet_ui` installed on the demo server and confirmed rendering** — the Component Showcase screen loads and displays correctly. It also caught a real bug the same way the backend modules' install-log loop always did: the showcase screen didn't scroll on mobile (Odoo's mobile action-manager layout expects a client action's own root to provide its scroll, rather than the page/body scrolling — fixed with an explicit `height: 100%; overflow-y: auto` on the container). Also worth noting separately: installing this module was unusually slow (~64s registry reload vs. single-digit seconds for prior single-module installs) — the working theory is that adding new files to the `web.assets_backend` bundle forces Odoo to invalidate and recompile that entire bundle, not just this module's assets, which is a heavier one-time cost than any backend-only module in this project has triggered before. **Confirmed, not just theorized, after the Phase C Slice 2–4 deploy** (Dispatch Board/Fleet Command Center/Copilot Rail, which added more files to the same bundle): `docker logs`/`top`/`docker stats` on the demo server showed the host and containers both idle (load average ~0.05, 8GB+ free, Odoo at 0.16% CPU) at the moment the site felt slow, and the Odoo log showed exactly two `Generating a new asset bundle attachment ... web.assets_web.min.{css,js}` lines at that timestamp, with only those two requests taking ~2–3s total — a request 10 minutes later was back to ~37ms. This is a one-time, self-resolving cost of adding files to `web.assets_backend`, not a capacity problem or a code regression — safe to expect and ignore (not investigate as a bug) on any future deploy that adds new files to this bundle.

**Phase B / Slice 1 (Command Palette) is built.** Rather than building a second Ctrl+K overlay, `deployfleet_ui` registers a `command_provider` into Odoo's own existing built-in command palette (`@web/core/commands/`) — searching shipment reference numbers, vehicle plates/names, and driver names, opening the selected record's form view directly. This is a deliberate scope correction from the original Phase B plan (a from-scratch Command Palette overlay): Odoo already ships that overlay, focus trap, and keyboard navigation, so competing with it on the same shortcut would be redundant and risky. See [18-component-library.md](docs/architecture/18-component-library.md)'s "Implementation note on the global Command Palette."

**Phase B / Slice 2 (Domain Mega Menus) is built.** One real shared `MegaMenu` OWL component — full-screen Command-Layer glass, client-side instant search, a responsive tile grid — parameterized by six curated domain content sets (Fleet & Vehicles, Dispatch & Trips, Compliance, Billing & Finance, Driver & HR, AI & Intelligence), all sharing one `ir.actions.client` tag distinguished only by a `params.domain` key. Fixes the exact fragility found in DeployGuard's own five copy-pasted mega-menu files (doc 16 §3.1). Tile content (~4-8 screens per domain, ~39 tiles total) was curated from the actual 65-menu-item inventory across all 43 backend modules — extracted and cross-checked programmatically, not guessed — not every menu item is promoted to a tile; admin/config-only screens (AI provider config/budget/usage log, notification rules, the event bus, mobile devices, licenses) stay reachable through the standard menu for now. Reachable today as six new top-level menu entries (placed ahead of the existing granular items, purely additive — nothing removed or reparented); becomes overlay-triggered from the DeployFleet Launcher once Slice 3 builds it.

**Phase B / Slice 3 (DeployFleet Launcher) is built — with one deliberate scope correction from the original plan.** Doc 16 §3.2 originally called for replacing Odoo's native app-switcher grid by patching `web.NavBar`. That patch was not attempted: a bad xpath match against this exact Odoo 19 nightly's `NavBar` template could break the top navigation bar across the *entire product*, not just this feature, and that template's current structure cannot be verified against a live instance from this development environment — an unacceptable blast radius for a feature this discretionary. Instead, the Launcher ships as a self-contained, always-mounted overlay: a global `Alt+L` hotkey (not `Alt+Space`, which collides with Windows' own system-menu shortcut) plus a persistent corner trigger button, with a priority-workspace strip (`Alt+D/F/C/B/R/I` to each Mega Menu domain), a destination grid, and localStorage-backed recents/favorites — every part of the spec except the native-chrome replacement. A second implementation-discovered constraint: DeployFleet nests all 43 modules under one single app menu rather than DeployGuard's many separate top-level apps, so the destination grid is populated from the six Mega Menu domains rather than Odoo's own (nearly-empty) app list. Full reasoning in doc 16 §3.2 and the module's own README.

**Phase B (Foundation navigation) is now complete: design-system tokens, the component library's first slice, the Command Palette, the Mega Menus, and the Launcher all ship in `deployfleet_ui`.**

**Phase C / Slice 1 (Mission Control) is built — the first Phase C flagship screen, and the first screen built under an explicit mobile-first mandate.** The owner/manager Home Dashboard: a silent-unless-nonzero attention strip (doc 16 §3.3) built from five genuine parallel `searchCount` queries against field-verified models (unassigned shipments, expired/expiring-soon compliance documents, vehicles in breakdown, pending AI approvals — the last using the `ai` StatusPill variant, a correct, deliberate use of the reserved AI violet), three real KPI cards (Active Vehicles, Active Shipments, Confirmed Revenue), and quick links to all six Mega Menu domains. Every count is a genuine ORM query, not a placeholder — field names (`deployfleet.shipment.state`, `deployfleet.compliance.document.state`, `deployfleet.vehicle.status`, `deployfleet.ai.action.request.state`) were read directly from each model's source before writing these domains. Reachable at menu sequence 0 and via the Launcher's `Alt+H` shortcut, deliberately left unassigned in Slice 3 for exactly this. This slice also retroactively bumped the shared Button and StatusPill atoms' touch targets to 44px (previously 36px and ~20px) per the explicit mobile-first instruction — both are reused everywhere, so this is the highest-leverage fix; Phase B's Launcher/Mega Menu screens were not audited for touch-target sizing in this same pass, a worthwhile follow-up still open.

**Phase C / Slice 2 (Dispatch Board) is built.** The dispatcher's primary workspace (`static/src/dispatch_board/dispatch_board.js`): confirmed shipments awaiting assignment, tap-to-expand a card to call the existing `action_suggest_assignments()` scoring engine and show ranked driver/vehicle candidates, one-tap Confirm. Three deliberate scope corrections from doc 16's original description, all reasoned through against the real backend source before writing any frontend code (see doc 16 §7.9 for the full reasoning): **(1) tap-to-assign, not drag-and-drop** — HTML5 drag-drop has poor touch support, and the mobile-first mandate applies to every Phase C screen without exception, so building drag-drop as the primary interaction would directly contradict it; **(2) the real backend contract is a plain `UserError`**, not the structured three-way `{success, hard_block, override_required}` response doc 16 described from DeployGuard's Roster Board — verified directly in `deployfleet_dispatch_assignment.py`'s `action_confirm()` (base module) and its `deployfleet_dispatch_compliance` override before writing the frontend, then reconciled with a single override dialog that writes one operator-supplied reason to both `override_reason` and `compliance_override_reason` fields before retrying, rather than parsing the error message to guess which check fired; **(3) Workspace Layer, not Command Layer** — the one deliberate exception among every screen shipped so far (Mission Control/Mega Menus/Launcher are all Command Layer dark glass), since doc 16 Principle 1 requires sustained-work screens to stay flat and high-contrast for an 8-hour shift rather than fight a translucent card. An urgency indicator (left-border stripe color, plus a pulsing `df-pulse-ambient` animation on shipments already past pickup) flags what needs attention first. Not yet built: the persistent candidate side-panel, bulk auto-assign, right-click context actions, and the ambient AI Recommendation Card doc 16 describes alongside the rule-based suggestions — all gated on the Copilot Rail/Console work.

**Phase C / Slice 3 (Fleet Command Center) is built.** The fleet manager's consolidated vehicle view (`static/src/fleet_command_center/fleet_command_center.js`): a status-filterable vehicle list (chips with live counts — All/Available/Assigned/Maintenance/Breakdown), each card expanding on tap into a genuine consolidated read across four models — current trip (`deployfleet.trip`), compliance documents (`deployfleet.compliance.document`, color-coded valid/expiring/expired), maintenance schedules (`deployfleet.maintenance.schedule`, due vs. on-schedule), and the last 5 fuel logs (`deployfleet.fuel.log`, anomalies flagged) — plus working one-tap status actions (Mark Available / Send to Maintenance / Report Breakdown) wired to the vehicle's real `action_set_*` methods. Same transparency discipline as the Dispatch Board: this reuses the same tap-to-expand accordion pattern rather than doc 16's originally-described sidebar + tabbed table + slide-in Inspector, since no Drawer/Inspector component exists yet; no Truck Health Ring or Profit Waterfall either — both remain "Aspirational" in doc 18, gated on a Fleet Score/per-vehicle revenue computation that doesn't exist yet. Workspace Layer, same reasoning as the Dispatch Board (sustained-work screen, not a launch screen).

**Phase C / Slice 4 (Copilot Rail — ambient half) is built, completing Phase C.** A persistent, collapsed-by-default docked tab on the right edge (`static/src/copilot_rail/copilot_rail.js`), violet-accented Command Layer glass, mounted globally via `main_components` (the same pattern as the Launcher) so it's present across every workspace, toggled with a new `Alt+A` keybinding (doesn't collide with anything already claimed — L, D, F, C, B, R, I, H) or the tab itself. Shows a live badge count of `deployfleet.ai.action.request` records in `pending_approval` state; expanding it opens a real approval queue — one card per request (feature, action type, target model, full proposed-values payload) with working Approve/Reject buttons wired directly to the existing, unmodified `action_approve()`/`action_reject()` pipeline (§4's mandatory suggestion → permission-check → human-approval → execute → audit chain — this UI adds no new execution path). Reject requires a typed reason in this UI as a deliberate product decision for a better audit trail (the backend's own `reason` parameter is optional). **Scope boundary, not a gap:** doc 16 §11 places the Copilot Console deliberately at the Phase C/D boundary — the *ambient* half (this rail) is Phase C and is what shipped; the *destination* half (a dedicated Copilot Console screen: agent catalog, usage/cost dashboard over `deployfleet.ai.usage`, per-record contextual awareness, a natural-language query interface, AI Recommendation Cards inline on the Dispatch Board/Fleet Command Center/Billing) is explicitly Phase D.

**Phase C (flagship screens) is now complete: Mission Control, the Dispatch Board, the Fleet Command Center, and the Copilot Rail's ambient half all ship in `deployfleet_ui`.** The Phase D Copilot Console remains the single largest gap between backend sophistication (a full six-agent catalog with a mandatory approval pipeline) and frontend reality anywhere in the product, per doc 16 §7.16 — now a smaller gap than before this session, since the approval-queue half of it already works.

**Real bug found and fixed during that same click-test pass: Mission Control, the Dispatch Board, and the Fleet Command Center all crashed on open** (`OwlError: Invalid props for component '...': unknown key 'action', unknown key 'actionId', unknown key 'updateActionState', unknown key 'className'`), reported by the user via three screenshots. Root cause: all three components declared `static props = {};`, which tells OWL "this component accepts zero props" and turns on strict validation — but every `ir.actions.client` root component Odoo's action manager mounts gets standard props (`action`, `actionId`, `updateActionState`, `className`, ...) injected into it regardless. The two screens that already worked (the Component Showcase, the Domain Mega Menu action) never declared `static props` at all, which is exactly why they didn't hit this — omitting `props` skips validation instead of declaring an empty schema. Fixed by removing the `static props = {}` line from all three files, matching the already-working pattern; the Launcher and Copilot Rail were not affected since they're `main_components` (mounted with the literal `{}` props the registry entry specifies, not the action manager's injected props) where the empty declaration is actually correct. This was caught only because real devices were used to click-test the deploy, exactly per §5's "test the golden path... in a browser before reporting complete" — none of this would have been caught by the syntax-only verification (`node --check`, XML well-formedness) this module has relied on so far, since `static props = {}` is syntactically and structurally valid OWL, just wrong at runtime against the action-manager contract. Worth carrying forward to any future `ir.actions.client` component in this module: either omit `static props` entirely, or declare it with the actual injected keys marked optional — never an empty object.

**Command Layer retrofitted from dark glass to light glass, project-wide — an explicit user decision ("only light backgrounds, as much as possible"), applied retroactively rather than just to new work.** Every Command Layer screen (the Launcher, the six Mega Menus, Mission Control, the Copilot Rail) originally shipped with a dark translucent gradient panel — light text on near-black glass, DeployGuard's own Enterprise OS Launcher recipe. All four, plus the shared `Card` atom's `command` variant, were retrofitted to a light, near-white translucent panel (`--df-glass-panel-bg`, a new theme-aware token in `tokens.scss`) with dark text, keeping the rest of the glass recipe (scrim, blur, saturation boost, hairline border, motion) unchanged. `--df-glass-border` was also relightened for the same reason; both tokens flip back to their original dark-panel values under `prefers-color-scheme: dark`/`data-theme="dark"`, the same way the rest of the neutral scale already does — so this is a genuine design-system revision (doc 16 Principle 1 / doc 17 §6 both updated with changelog notes), not a one-off screen hack. Caught and fixed in the same pass: `--df-color-neutral-600` was already referenced throughout the Dispatch Board and Fleet Command Center's SCSS but was never actually defined in `tokens.scss` — a latent bug (an invalid CSS custom property reference silently no-ops the declaration) that would have quietly fallen back to inherited text color rather than the intended muted gray. Any future Command Layer surface should default to the light recipe.

**Phase D / Slice 1 (Copilot Console) is built.** The destination screen for working the AI investment in bulk (`static/src/copilot_console/copilot_console.js`), complementing the Copilot Rail's ambient approval queue: an **Agent Catalog** (all six `deployfleet.ai.agent` personas, each showing its description, related models, model tier, and data category, plus a real, working Enable/Disable toggle wired to the underlying `deployfleet.ai.feature.enabled` field — the per-feature switch CLAUDE.md §4 makes non-negotiable) and a **Usage & Cost Dashboard** over `deployfleet.ai.usage` (this month's cost/tokens via the model's own `total_cost_this_month()`, an all-time cache-hit rate, a per-feature cost/token breakdown via `read_group`, and a recent-calls log) — real ORM queries and a real field write, not mockup data. Command Layer, light glass per the retrofit above, since doc 16 §7.16 itself calls this a command-center-class destination screen, the same category as Mission Control. **Scope cut, same transparency discipline as every prior slice:** doc 16 §7.16 also calls for each catalog entry to show "recent output" and "confidence" — deferred, since the four Phase 5 prediction models each use a different schema for their own risk/confidence metric (e.g. `risk_score`/`risk_level` on `deployfleet.maintenance.prediction`), and unifying that into one generic display would need per-agent custom rendering logic rather than a generic read.

**Phase D / Slice 1 also finishes the natural-language query interface and the AI Recommendation Card — the two other items doc 16 §7.16 and §11 explicitly named as deferred.** Each Agent Catalog card now has a real "Ask" box wired to `deployfleet.ai.core.complete(feature.key, agent.system_prompt_template, question)` — the exact same mandatory-pipeline entry point (policy/permission/budget checks, response cache, provider call, usage logging) every other AI feature goes through, not a new shortcut path. Scoped per-agent rather than as one generic global query box, since each agent already owns its own system prompt and model tier. Separately, `DeployfleetAiRecommendationCard` is a genuinely new atom (distinct from the existing inline `AiBadge` marker) with its first real consumer wired into the Fleet Command Center's expanded vehicle detail, surfacing `deployfleet.maintenance.prediction`'s risk score/level/basis — silent when `risk_level` is "low," matching Mission Control's attention-strip discipline. One deliberate deviation from doc 18's "exactly two actions (Accept / Dismiss)" spec: shipped as open-ended `actionLabel`/`onAction` plus `onDismiss` rather than a hardcoded pair, since a maintenance risk score has no single concrete "accept" write the way a Dispatch Board suggestion does — documented in doc 18's own implementation note. **Still deferred, and now the only remaining Phase D gap:** per-record contextual awareness for the Rail/Console (neither yet knows which record the user has open elsewhere in the app — this needs a safe, verified hook into the action manager's current controller, deliberately not attempted against an unverified internal API in this exact Odoo 19 nightly, the same risk-aversion reasoning that kept the Launcher off a `web.NavBar` patch) and AI Recommendation Cards on the remaining flagship screens (Dispatch Board, Billing — Fleet Command Center is the first consumer, not the last).

**Launcher/Copilot Rail glassmorphism and mobile polish pass, per direct user feedback** ("needs polish and to be truly glassmorphic and mobile friendly"). The Launcher and Copilot Rail are the only two true overlay surfaces in `deployfleet_ui` (scrim + panel sitting on top of other page content) — Mission Control/Mega Menus/Copilot Console are full-page screens with nothing behind them to blur. Both previously applied `backdrop-filter` only to the scrim, never to the panel itself, so the panel read as a mostly-opaque light card, not frosted glass. Fixed by giving both panels their own `backdrop-filter` (plus the `-webkit-backdrop-filter` prefix Safari/iOS still requires — without it, blur silently no-ops on a large share of the mobile-first target market), an inset top/edge highlight for the light-catching-glass look, and lowering the shared `--df-glass-panel-bg` token from 0.92 to 0.82 opacity (0.85 → 0.78 in dark mode) so blur actually reads through. Also fixed a real mobile *functional* bug in the same pass, not just a visual one: the Launcher's per-tile favorite-star button was hidden behind `:hover` with no fallback — on a touchscreen there is no hover state, so the button was permanently unreachable on the phones/tablets this product is built mobile-first for. Now scoped to `@media (hover: hover)` so hover-to-reveal only applies on genuinely hover-capable pointers; the star is always visible by default on touch. Other mobile fixes: search input/close button/chips/priority-pills all bumped to the established 44px touch-target minimum (previously as small as 26–36px); the header forces search onto its own full-width row below 640px; the destination grid drops to one column below 400px; overlay padding tightens on mobile with an `env(safe-area-inset-bottom)` allowance for notched devices.

**Phase E started per doc 16 §11 ("Premium experiences"), at the user's explicit direction — with one scope note surfaced before writing speculative code.** Doc 16 §11 lists Phase E as: Live Fleet Map, Animated Route Timeline, Load Builder, Fleet Heat Map, Driver Scorecards, full analytics/Insights depth, and mobile refinement across all three apps — explicitly gated in the doc's own language on "once the desktop/tablet flagship work has validated the patterns," and this jumps ahead of doc 16's own "Phase D — Operational workspaces" bucket (Maintenance Intelligence, Billing/Financial Intelligence, Driver Workspace, Customer Workspace, Compliance, Workshop UI), which remains entirely unbuilt. Several Phase E items are also explicitly gated on backend capabilities in the architecture docs themselves — Live Fleet Map and Fleet Heat Map need a real GPS/position feed, Load Builder is explicitly gated in doc 16 §7 on the multi-stop shipment model maturing per [12-ltl-freight-management-architecture.md](docs/architecture/12-ltl-freight-management-architecture.md), and "mobile refinement across all three apps" refers to the driver/dispatcher/customer React Native apps in [MOBILE_ARCHITECTURE.md](MOBILE_ARCHITECTURE.md), a different codebase/stack entirely from `deployfleet_ui`'s Odoo web client. Verified before writing any frontend code, the same discipline every prior slice in this project has followed: `deployfleet.vehicle`/`deployfleet.trip` have zero GPS/position fields anywhere (confirmed by grep across both modules), and `deployfleet.trip.shipment.line` is just "which shipments are on this trip," no stop sequencing — so Live Fleet Map, Fleet Heat Map, and Load Builder are genuinely not buildable against real data right now, not merely deprioritized.

**Phase E / Slice 1 (Driver Scorecards) is built** — the one Phase E item with a complete, real backend already in place. `static/src/driver_scorecards/driver_scorecards.js`: a score-filterable driver list (chips: All/Good 80+/Watch 50-79/At Risk <50, live counts) ordered worst-score-first so drivers most worth attention surface without a manual sort. Tapping a driver reuses the same accordion pattern as the Dispatch Board and Fleet Command Center, showing their real recent `deployfleet.driver.performance.event` history (accidents, violations, harsh braking, speeding, fuel abuse, late deliveries) alongside `deployfleet_driver_performance`'s genuine computed `hr.employee.deployfleet_reliability_score` (a trailing-365-day roll-up, not a placeholder), years of experience, accident count, and license-expiry status. Workspace Layer, same reasoning as the Dispatch Board and Fleet Command Center. **Deliberately not the full driver-360** doc 16 §7.14 describes: no consolidated trip/current-assignment view, no compliance traffic-light chips beyond the license-expiry badge, and no Driver Performance Radar multi-axis chart — that needs a real charting-library evaluation, one of the few places doc 18 itself calls for a real charting primitive over a hand-rolled visual, deliberately not attempted speculatively in this slice.

**Real bug found and fixed after the user reported a database-wide "Style error. The style compilation failed" blocking every screen** (not just Driver Scorecards — Odoo compiles the entire `web.assets_backend` SCSS bundle as one unit, so one broken file breaks every screen that shares the bundle, which explains why the Dispatch Board looked unstyled too even though its own SCSS was fine). Root cause: `launcher.scss`'s mobile safe-area padding used `max(var(--df-space-1), env(safe-area-inset-bottom))` — but (lib)sass ships its own built-in `max()` math function under the same name as the CSS one, and it shadows the native CSS function, trying to evaluate `var(--df-space-1)` as a literal Sass number and failing the entire bundle's compile with `"var(--df-space-1)" is not a number for 'max'`. This is exactly the class of bug the brace-balance check this module has relied on (`grep -o '{' | wc -l` vs `grep -o '}' | wc -l`) structurally cannot catch — braces were perfectly balanced, the file just wasn't valid Sass. Fixed by switching to `calc(var(--df-space-1) + env(safe-area-inset-bottom, 0px))` — `calc()` has no such name collision (already used safely elsewhere in this module, e.g. `metric_card.scss`) and additive stacking is the more common safe-area-inset pattern anyway. **New verification capability, worth carrying forward:** `pip install libsass` was available in this environment and gives a genuine SCSS compiler (`python3 -c "import sass; sass.compile(filename=...)"`) — every future SCSS change in this module should be compiled with it (both individually and as the full concatenated `web.assets_backend` bundle in manifest order, which is what actually caught this) before being considered verified, not just brace-balance-checked. All 19 of this module's SCSS files, individually and concatenated in manifest order, now compile cleanly.

**Real bug found and fixed: the Copilot Console crashed on open** with `OwlError: ... "this.orm.readGroup is not a function"`, reported by the user via screenshot. Another instance of this exact Odoo 19 nightly's version drift, the same class of issue as the earlier `res.groups.category_id`/`res.users.groups_id` renames — the JS ORM service's convenience wrapper for `read_group` isn't exposed as `readGroup` on this build (or isn't exposed at all). Rather than guess the correct current name, fixed by removing the dependency entirely: the per-feature cost/token breakdown now fetches up to 2000 `deployfleet.ai.usage` records via the already-confirmed-working `searchRead` and aggregates them client-side with a plain JS reduce, producing the exact same `{feature, __count, estimated_cost_usd, tokens_in, tokens_out}` shape the template already expected — no XML changes needed. The user separately re-shared the earlier Fleet Command Center `static props` screenshot; confirmed it's the same timestamp as the bug already fixed in commit `95dd001`, not a new regression — just needs redeploying if that commit hasn't landed on the server yet.

**Proposed next step:** re-deploy (this fix plus the earlier style-compilation fix, if not already live) and re-click-test Driver Scorecards, the Copilot Console (agent toggles, usage dashboard, "Ask this agent"), and Fleet Command Center on the demo server, alongside re-confirming the Launcher/Copilot Rail glass and mobile fixes on a real touch device (the favorite-star fix specifically needs a real touchscreen to verify, not just a resize of a desktop browser window), and re-checking the Dispatch Board actually has confirmed shipments to show once styling is back — the "No shipments are waiting on dispatch right now" the user saw may simply be the correct empty state (demo shipments may have already progressed past "confirmed" through the dispatch → trip → delivery pipeline) rather than a bug, worth confirming against the real demo data rather than assumed either way. The remaining Phase E items stay blocked on real backend work, not frontend effort: a GPS/position feed for Live Fleet Map/Fleet Heat Map, and the multi-stop shipment model for Load Builder — both are backend/data-model decisions, not something to fake from `deployfleet_ui`. Phase D's own remaining gap (per-record contextual awareness for the Copilot Rail/Console) and the still-open items below (hard risk #5's domain-model validation, the Odoo edition decision, the SSH password rotation) remain worth picking up whenever, not blocking.

**Now moving into custom-views work, at explicit user direction ("we want to use custom views as much as possible") — starting with a full audit of Fleet & Vehicles before any UI is designed.** Per CLAUDE.md §5's existing mandate ("avoid excessive standard Odoo list/form views where a purpose-built interface materially improves the workflow"), this is a deliberate escalation of that principle specifically for the Fleet & Vehicles domain, not a new one — the plan is audit first (every model, field, action, and existing view in `deployfleet_vehicle` plus every module that extends it), then design the UI/flows collaboratively with the user before writing any component, the same research-before-code discipline every prior slice in this project has followed.

**Audit complete; three structural questions resolved with the user via explicit choice before any code was written** (per "we'll plan the ui and flows together"): (1) screen structure — a **mixed** approach: Workshop gets its own dedicated board (its state-machine workflow doesn't fit a vehicle-centric view), while Tyres/Insurance/Parts-and-Workshop-summary all fold into a deepened Fleet Command Center "Vehicle 360" instead of each getting a separate screen; (2) the two disconnected fuel-anomaly signals (`deployfleet.fuel.log.is_anomaly`'s simple threshold vs. the separate, statistically distinct `deployfleet.fuel.anomaly` AI model) — **reconcile into one indicator** rather than showing both; (3) Parts and Assets, which are NOT vehicle-linked (a shared stock pool and free-text-location equipment registry respectively) — **keep them under the Fleet & Vehicles domain but make them visually distinct** (registry/inventory styling) rather than forcing them into the vehicle-centric pattern. Key backend facts grounding this work, verified by reading source before planning: `deployfleet.workshop.job.card`'s `action_close()` mutates vehicle status as a side effect (`action_set_available()`) and consumes part stock; Insurance auto-syncs a linked compliance document; Parts/Assets have zero vehicle FK. Full model-by-model detail lives in this session's audit notes, not yet transcribed into doc 16 — worth doing before this thread is considered closed.

**First deliverable of that plan, the Workshop Board, is built** (`static/src/workshop_board/workshop_board.js`) — see doc 16 §7.12's "Current state (built)" and the module README for the full writeup. Same tap-to-expand/filter-chip pattern as every Workspace Layer screen this project has shipped, not a literal drag-drop kanban, for the same mobile-first reasoning as the Dispatch Board. The Fleet & Vehicles Mega Menu's "Workshop" tile now points at this board instead of the stock job-card list.

**Second deliverable, the Fleet Command Center deepened into a "Vehicle 360," is built.** Three new sections joined the existing Trip/Compliance/Maintenance/Fuel detail: **Tyres** (every non-scrapped `deployfleet.tyre` — position, tread depth, fitted/retreaded state), **Insurance** (latest `deployfleet.insurance.policy` plus an open-claims count from `deployfleet.insurance.claim`), and **Workshop** (open, non-closed `deployfleet.workshop.job.card` records with running cost). Parts was deliberately left out — confirmed by source read it has zero `vehicle_id` — and stays a separate registry screen per the agreed decision. Also reconciles the two independent fuel-anomaly signals (`deployfleet.fuel.log.is_anomaly`'s flat 30%-threshold boolean vs. the separate, stricter `deployfleet.fuel.anomaly` z-score model from Phase 5) into one badge per fuel-log row: flagged if either signal fires, showing the z-score when the AI model scored that log or a plain "Anomaly" label otherwise. Verified: XML well-formed, JS syntax via `node --check`, full `web.assets_backend` SCSS bundle compiled via libsass (individually and concatenated in manifest order), security groups checked (`group_deployfleet_dispatcher` already has read access on all four newly-read models — no access gap). See doc 16 §7.10 for the full writeup.

**Fifth real bug found and fixed, reported by the user via screenshots: Driver Scorecards crashed on open** with `ValueError: Cannot convert hr.employee.deployfleet_reliability_score to SQL because it is not stored`. Unlike the prior four bugs, this one was this module's own mistake, not Odoo-19-nightly version drift: `driver_scorecards.js`'s `loadDrivers()` asked `searchRead` to `order: "deployfleet_reliability_score asc"`, but that field is a plain computed `Float` with no `store=True` on `hr_employee.py` (`deployfleet_driver_performance` module) — this Odoo build refuses to build a SQL `ORDER BY` against an unstored computed field. Fixed entirely client-side: dropped the `order` param and sort the returned array worst-score-first with a plain JS `.sort()` after mapping each driver's score band, no backend model change needed for what's purely a frontend display concern. Grepped every other `order:` usage across `deployfleet_ui` after this fix and confirmed no other screen has the same class of bug (the other computed-field order params — `is_due` on maintenance schedules — are all backed by `store=True`).

**Third and fourth deliverables, the Parts Registry and Asset Registry, are built — completing the agreed Fleet & Vehicles custom-views plan.** Both `deployfleet.part` and `deployfleet.asset` have zero `vehicle_id` (confirmed by source read), so per the agreed decision both stay dedicated screens, but styled as a dense registry ledger (header row, table rows, right-aligned tabular-nums figures) rather than the floating-card/accordion pattern every other Fleet & Vehicles screen uses — a deliberate second visual dialect within Workspace Layer, "a ledger to scan" vs. "a queue to work." **Parts Registry** (`static/src/parts_registry/parts_registry.js`): every part with quantity/reorder level/unit cost, filterable to Low Stock; a real "Receive Stock" quick-action wired to `action_receive_stock()` (stock consumption is deliberately not duplicated here — it already happens as a side effect of Workshop/Tyres closing their own records). **Asset Registry** (`static/src/asset_registry/asset_registry.js`): every asset filterable by status, with one-tap status-transition actions wired to the asset's real `action_set_*` methods. Both replace their Fleet & Vehicles Mega Menu tile's previous target (the stock list view). Verified: XML well-formed, JS syntax via `node --check`, full `web.assets_backend` SCSS bundle compiled via libsass (22 files, individually and concatenated in manifest order), ruff/pylint clean, security groups checked (`group_deployfleet_dispatcher` has read+write on `deployfleet.part`, read+write+create on `deployfleet.asset` — both sufficient for this screen's actions). See doc 16 §7.12a for the full writeup.

**This closes the agreed Fleet & Vehicles custom-views plan**: Workshop Board → Vehicle 360 → Parts/Asset Registries, all four deliverables shipped in order. **Correction to an earlier note in this section:** the "Insurance is absent from the Fleet & Vehicles Mega Menu" gap flagged a few entries above was wrong — re-checked `domain_content.js` directly and Insurance has had a tile under the **Compliance** Mega Menu domain since that domain was first built, not just its own top-level menu. Whether Insurance should *also* get a tile under Fleet & Vehicles now that Vehicle 360 surfaces it prominently is a legitimate open design question (it's arguably both a compliance concern and a fleet concern) — raised to the user directly, who chose to add it. **Insurance now has a tile in both the Compliance and Fleet & Vehicles Mega Menu domains** (same `deployfleet_insurance.action_deployfleet_insurance_policy` action, deliberately duplicated — the first tile in the product to appear in two domains, a considered exception to the "no tile in two domains" default rather than an unreviewed one). None of this session's Fleet & Vehicles work (Workshop Board, Vehicle 360, Parts/Asset Registries, the Insurance tile) has been verified in a live browser yet — worth redeploying and click-testing all of it on the demo server before considering this thread fully closed, per the same "syntax-valid is not the same as rendering correctly" caveat this module has carried since Phase A.

**A new governing rollout strategy has been set, at explicit user direction, formalized as [docs/architecture/20-experience-implementation-strategy.md](docs/architecture/20-experience-implementation-strategy.md).** The Fleet & Vehicles work above proved the pattern well enough that the user asked for it to become a permanent product principle: DeployFleet should not feel like an Odoo ERP, custom workspaces are a sales mechanism (not just polish), and — the concrete rule — every operational domain entity gets a custom view, including simple master data, with domains finished one at a time to completion rather than built one screen-type at a time across every domain. This **supersedes doc 16 §11's original horizontal Phase A–E rollout** (doc 16 §7's area-by-area redesign direction and doc 18's component catalog remain valid; only the sequencing logic changes). Doc 20 also re-audits Fleet & Vehicles against the tightened rule and finds five remaining gaps the first four deliverables didn't cover, because they replaced review/quick-action surfaces, not record-level create/edit surfaces: **Vehicle Profile** (the actual `deployfleet.vehicle` form — today zero tabs, zero related-record rollups), **Vehicle Types Workspace** (nine fields, still a bare stock editable list), **Fuel Intelligence** (a fleet-wide trend/anomaly view, not just the existing log list/form), **Tyre Manager** (fleet-wide inventory/replacement planning — today only reachable via a stock list or read-only inside one vehicle's Vehicle 360 detail), and **Insurance Center** (fleet-wide renewal/claims view, same gap shape as Tyres). Doc 20 §8 also raises two open domain-boundary questions for the user — whether Maintenance should split into its own top-level Mega Menu domain, and whether a Customers domain should be carved out of Billing & Finance/Dispatch & Trips — deliberately left undecided rather than silently restructured. **Next step:** get the user's confirmation on the five-gap Fleet & Vehicles list (and the two open domain-boundary questions) before building anything, per doc 20 §5's audit-then-design-together-then-build sequence — the same collaborative-planning discipline the original four Fleet & Vehicles deliverables used.

**User confirmed all three open questions**: build all five gaps as one batch (not sequenced), keep Maintenance inside Fleet & Vehicles (no domain split), keep the current Billing & Finance/Dispatch & Trips structure (no Customers domain carved out) — both domain-boundary questions resolved as "no restructuring for now."

**All five Fleet & Vehicles gaps are built, closing this domain to nine-of-ten workspaces ✅ (only Maintenance Schedules/the Maintenance Planner widget remains, deliberately out of scope for this batch — see doc 20 §3/§6).** **Vehicle Profile** isn't a separate screen: rather than build a second `ir.actions.client` with unverified per-record `params` plumbing (a real, new risk pattern this project hadn't used before, given its history with Odoo action-manager surprises — the `static props = {}` bug being the clearest precedent), Fleet Command Center's already-working expanded card gained a real "Identity & Capacity" edit section (license plate, model, vehicle type, driver, odometer, capacity fields, saved via `orm.write`) — the safer, more consistent choice. **Vehicle Types Workspace** (`static/src/vehicle_types_workspace/`) replaces the stock editable list with a registry-ledger screen; building it surfaced a real pre-existing gap, not a frontend bug — `deployfleet.vehicle.type` write access was `base.group_system`-only, meaning fleet managers couldn't manage their own vehicle classes at all, fixed with a `group_deployfleet_manager` ACL grant in `deployfleet_vehicle`'s own access file (mirroring the existing Part Category ACL shape). **Fuel Intelligence** (`static/src/fuel_intelligence/`) is a fleet-wide, anomaly-reconciled fuel-log ledger with a real "Log Fuel" quick-add form. **Tyre Manager** (`static/src/tyre_manager/`) is a fleet-wide, worst-tread-first tyre list with real rotate/retread/scrap/record-reading actions — building it found and fixed a genuine pre-existing backend ACL bug: dispatcher lacked `create` on `deployfleet.tyre.event`, which `action_rotate`/`action_retread`/`action_scrap` all create internally, despite dispatcher already having `write` on the parent `deployfleet.tyre` model — an AccessError waiting to happen that the existing test suite never caught since it runs as the superuser. Fixed with a one-line ACL grant plus a new regression test (`test_dispatcher_can_rotate_tyre`) that exercises the fix under a real dispatcher-group user, not just the superuser. **Insurance Center** (`static/src/insurance_center/`) is a fleet-wide, expiry-banded policy ledger with a real "File a Claim" form and the claim lifecycle actions; dispatcher can file a claim but not progress it (`create=1`/`write=0` on `deployfleet.insurance.claim`) — judged a legitimate approval gate rather than a bug (unlike the tyre.event case, dispatcher has no pre-existing write access on the parent model implying otherwise) and documented as such, not silently "fixed." All five Mega Menu tiles now point at these screens instead of stock views, including both of Insurance's tiles (Fleet & Vehicles and Compliance domains). Verified throughout: XML well-formed, JS syntax via `node --check`, full `web.assets_backend` SCSS bundle compiled via libsass (26 files, individually and concatenated in manifest order), ruff/pylint clean, all touched ACL CSVs parse correctly, new tests added for all four new actions' menu/action registration plus the tyre.event ACL regression.

**None of this batch has been verified in a live browser yet** — same caveat as everything before it in this module. Worth redeploying and click-testing all nine Fleet & Vehicles screens (plus the four from before) on the demo server, particularly the three new inline-form patterns introduced this round (Vehicle Types' inline edit, Fuel Intelligence's "Log Fuel" form, Insurance Center's "File a Claim" form) and the `fleet.vehicle.model`-backed select in the new Vehicle Profile section, none of which have a prior verified-in-browser precedent in this module. **Genuinely remaining in Fleet & Vehicles:** the Maintenance Planner (doc 16 §7.11's calendar-plus-Gantt widget) — a real widget-shaped build, sized differently from the five registry/accordion screens that just shipped, deliberately not attempted in this round.

**Maintenance Planner is built, closing Fleet & Vehicles to ten-of-ten workspaces ✅ — the domain is now complete.** Built to a detailed product brief from the user (Attention Strip, week/month Calendar, Gantt-style Timeline, Vehicle Health Cards, AI integration, analytics KPIs, mobile considerations), but audited against the real backend line-by-line before building anything, the same discipline that ruled out Live Fleet Map in Phase E. Several brief items had no backing data and were dropped rather than faked, documented explicitly in doc 20 §6a: `deployfleet.maintenance.prediction` has no predicted-failure-date field (only risk_score/risk_level/basis) so nothing is plotted on a calendar as if it had a specific date; no inspection model exists anywhere in the 43-module backend; `deployfleet.workshop.job.card` has no technician/priority/duration fields, so Timeline bars are day-granularity spans, not hour-level Gantt bars; "workshop capacity" has no staffing model behind it, so a plain open-job-card count is shown instead of a fabricated percentage; "which maintenance can be grouped together" has no grouping dimension (depot, type) on the schedule model and wasn't attempted; "Reschedule"/"Assign Workshop"/"Send Driver Notification" aren't real backend actions — the only real lever for "rescheduling" is `action_record_service`, since `next_due_date` is a stored compute, not directly writable.

One cohesive workspace (`static/src/maintenance_planner/maintenance_planner.js`), three internal tabs rather than separate menu items: **Overview** (attention strip, six real KPIs including an honestly-labeled approximate cost-per-km derived from summed fuel-log distance deltas, AI recommendations, a worst-first filterable Vehicle Health Card grid); **Calendar** (a hand-rolled CSS-grid week/month view — no charting library); **Timeline** (a Gantt-style view with percentage-positioned job-card bars and due-date markers, no drag-resize, same mobile-first reasoning as the Dispatch Board). Health Score is a `deployfleet_ui`-computed heuristic (documented formula, real input signals) — not a backend field, unlike every other score this module has surfaced. AI recommendations follow the mandatory suggestion → human-approval → execute pipeline; Workshop Board stays the sole execution surface, this screen never duplicates its state-advance buttons.

Two OWL-template risk points were caught and avoided before they could become the module's fifth/sixth real bug: the `??` nullish-coalescing operator has no prior precedent inside an XML template expression in this module (only ever used in plain `.js` files) and was replaced with JS getters; an inline array literal in `t-foreach` was replaced with a component-level property, matching how every other `t-foreach` in this module iterates a real property. Verified: XML well-formed, JS syntax via `node --check`, full `web.assets_backend` SCSS bundle compiled via libsass (27 files, individually and in manifest order), ruff/pylint clean, security groups checked (`group_deployfleet_dispatcher` has sufficient access across all five models this screen touches). Replaces the Fleet & Vehicles Mega Menu's "Maintenance" tile.

**Not yet verified in a live browser — and this is the highest-priority screen in the whole module to click-test first**, more so than any prior slice: it's the largest, most novel component built in `deployfleet_ui` to date (three internal views, a hand-rolled calendar grid, percentage-positioned Gantt bars), none of which have a prior working-in-a-real-browser precedent to lean on. **This closes the Fleet & Vehicles custom-views initiative in full** — the first domain to reach complete status under doc 20's domain-completeness-first rollout.

**Driver & HR is the second domain tackled, at the user's explicit direction ("next let's tackle Driver & HR then we will do Dispatch and trips after").** Audited every model in the domain (`deployfleet_driver`, `deployfleet_driver_performance`, `deployfleet_driver_advance`, `deployfleet_leave`, `deployfleet_payroll`, `deployfleet_loans`) including every security ACL before building anything, the same discipline as Fleet & Vehicles. Confirmed via `AskUserQuestion`: Payslips+Loans combine into one Payroll Center; the leave-request write-permission asymmetry gets fixed as a real bug (not left as a designed gate); Advances get both a fleet-wide ledger and a Driver 360 section; build all four in one pass.

**Two real, pre-existing backend issues found and fixed, not just frontend gaps:**
1. `deployfleet.leave.request` granted the driver group `create=1` but `write=0` — a driver could file a leave request but could never call `action_submit()` on it themselves. Fixed properly (not just an ACL flip, since a blanket write grant would've let any driver edit any *other* driver's leave request — there was no record-level scoping mechanism anywhere in this codebase yet): added `perm_write=1` in `deployfleet_leave/security/ir.model.access.csv` plus **this codebase's first `ir.rule`** (`deployfleet_leave_request_rule_driver_own`, `deployfleet_leave/security/deployfleet_leave_security_rules.xml`), scoping driver access to `[('employee_id.user_id', '=', user.id)]` — the standard Odoo self-service pattern. Two new regression tests confirm both halves (a driver can submit their own request; a driver still cannot write another driver's). This fix lands at the backend/ACL level for whichever driver-facing surface eventually needs it (the separate React Native driver app) — the Leave Planner workspace built in `deployfleet_ui` itself stays dispatcher/manager-facing, like every other screen in this module.
2. Payroll and Loans are gated to `group_deployfleet_hr_payroll_officer`, a role off the dispatcher→manager→owner chain entirely — a manager without that separate role has **zero ACL rows at all** on payslips/loans. Not a bug (looks like a deliberate confidentiality boundary, consistent with CLAUDE.md §4's payroll-sensitivity note), but a real hazard for a frontend screen: most users hitting Payroll Center via the Mega Menu would get an `AccessError` on the very first read. Handled with a clean access-denied state in the screen itself instead of an unhandled crash.

**Four workspaces built.** **Driver Scorecards deepened into the domain's Driver 360** (`static/src/driver_scorecards/`) — same "evolve the existing screen" choice as Fleet Command Center's Vehicle Profile: a real editable Identity & Qualifications section (license class/number/expiry, endorsements, qualified vehicle types, the legacy `deployfleet_risk_score` field), plus Advances and Leave sections with their real state-machine actions and a "Request Leave" quick-add form. Worth flagging: `deployfleet_risk_score`'s own help text claims it gets "lowered by driver-performance incidents once `deployfleet_driver_performance` is installed" — but that module actually built a wholly separate field (`deployfleet_reliability_score`, already surfaced) instead of wiring into this one; shown as plain editable data, not treated as a live metric. **Driver Advances** (`static/src/driver_advances/`) is a fleet-wide registry ledger, same visual dialect as Parts/Asset Registries — driver-linked data gets both a per-driver view (in Driver 360) and a fleet-wide planning ledger, mirroring the Tyre Manager/Insurance Center precedent. **Leave Planner** (`static/src/leave_planner/`) has three tabs: Requests (filterable + quick-add + the real state machine), Calendar (a hand-rolled month grid marking every day within an approved/submitted request's range — no separate Timeline needed since leave spans render directly in day cells), and Balances (a real read over `deployfleet.leave.balance`, a model with **no view of any kind anywhere in the backend before this**). **Payroll Center** (`static/src/payroll_center/`) combines Payslips and Loans per the user's choice, since they're both officer-gated and already linked in the backend (`action_confirm()` on a payslip calls each active loan's `action_apply_deduction()`).

**Mega Menu tiles repointed**: Drivers → Driver Scorecards/360, Driver Advances → the new registry, Leave → Leave Planner, Payslips+Loans collapsed into one Payroll Center tile. **Driver Performance's tile deliberately stays on the stock list — the one remaining gap in this domain** (Driver 360 shows one driver's own recent events; a fleet-wide "browse all incidents this week" ledger doesn't exist yet, deferred the same way Maintenance Planner was deferred out of Fleet & Vehicles' first batch).

Verified: XML well-formed, JS syntax via `node --check`, full `web.assets_backend` SCSS bundle compiled via libsass (30 files, individually and in manifest order), ruff/pylint clean on both `deployfleet_ui` and `deployfleet_leave`, all touched ACL CSVs parse correctly. **Not yet verified in a live browser** — same standing caveat as every screen in this module.

**Dispatch & Trips is the third domain tackled, at explicit user direction ("next let's tackle dispatch and trips").** Audited every model, state machine, view, and ACL across `deployfleet_dispatch`, `deployfleet_dispatch_compliance`, `deployfleet_trip`, `deployfleet_delivery`, `deployfleet_route`, and the dispatch-relevant parts of `deployfleet_customer` — including reading the already-shipped Dispatch Board (Phase C Slice 2) to see exactly what it covered before designing anything, since this domain started with one real workspace already in place rather than zero. Confirmed via `AskUserQuestion`: evolve the existing Dispatch Board into the full shipment lifecycle rather than building a separate Shipments screen; treat Routes'/Depots' dispatcher-read-only ACL as a bug and open write access; fix the missing driver-availability check in dispatch scoring now, as a real backend change; build the whole domain in one batch.

**Two real, pre-existing backend issues found and fixed:**
1. `deployfleet.route`/`deployfleet.route.stop`/`deployfleet.depot` granted `group_deployfleet_dispatcher` only `perm_read=1` despite their menus being dispatcher-visible — every create/edit action on the stock forms would have silently failed. Fixed with a `perm_write`/`perm_create` grant (still no `perm_unlink`, matching the codebase-wide convention that dispatcher never gets unlink anywhere).
2. `deployfleet.shipment._score_candidate()` (extended in `deployfleet_dispatch_compliance` with expired-document/rest-hour/consecutive-driving-day checks) never checked driver leave — a driver on approved leave overlapping the requested pickup date could still be suggested and confirmed. Fixed by adding `_driver_on_approved_leave()` at the same extension point, checking `deployfleet.leave.request` (added as a manifest dependency — no circular-dependency risk, since `deployfleet_leave` depends on `deployfleet_trip`, not on `deployfleet_dispatch_compliance`). Two new regression tests cover both the disqualifying and non-disqualifying cases. This closes the "Driver Availability" gap as a backend scoring fix, not a new screen — the Leave Planner already answers "who's on leave when" for browsing.

**Five workspaces built.** **Dispatch Board evolved** (`static/src/dispatch_board/`) — the screen originally scoped to confirmed-shipment matching now also covers booking (a "New Shipment" quick-add form creating drafts) and full-lifecycle tracking via state filter chips (Active/Draft/Confirmed/Assigned/In Transit/Delivered/Cancelled), the same "evolve, don't duplicate" choice as Fleet Command Center/Driver Scorecards; the original suggest-assignments/confirm/override flow is unchanged for confirmed shipments, and assigned/in-transit/delivered shipments get a read-only assignment summary pointing to Trip Board/Delivery Center rather than duplicating their functionality. **Trip Board** (`static/src/trip_board/`) has two tabs: Trips (the Workshop Board's filter-chips-plus-accordion pattern, with the real depart/complete/delay/cancel state machine — and the first UI path anywhere in the product for `action_complete()`'s `odometer_end` argument, which the stock form's bare zero-arg button could never set) and Calendar (a month grid reusing the Leave Planner's exact grid-building code, adapted to single-day pins — this closes the "Dispatch Calendar" gap doc 16 §1 flagged at zero `<calendar>` views anywhere in the product). **Delivery Center** (`static/src/delivery_center/`) is a fleet-wide proof-of-delivery ledger plus a trip-scoped quick-add form; `deployfleet.delivery.create()` is itself the completion event (no `action_*` method exists on the model), so this screen has no edit path by design. Signature/photo upload is the module's first `Binary`-field handling anywhere, done with a plain `<input type="file">` read via `FileReader` into base64. **Route Manager** (`static/src/route_manager/`) is a lane registry with inline stop management (add/remove, auto-incrementing sequence, no drag-reorder); confirmed by source read that `deployfleet.route` holds no GPS/coordinate data anywhere, so this is a lane-graph registry, not a map — the same GPS-less reality that already ruled out Live Fleet Map/Fleet Heat Map in Phase E. **Depot Registry** (`static/src/depot_registry/`) mirrors the Vehicle Types Workspace's simple-master-data pattern exactly (registry table, inline edit, delete available to every role with a denied write surfacing as a friendly notification).

**Also fixed while touching this area, found during the audit, not the build:** the Mega Menu's "Dispatch Board" tile had pointed at the **stock kanban action** (`deployfleet_dispatch.action_deployfleet_dispatch_assignment`) since Phase C — the custom OWL Dispatch Board built that same phase was only ever reachable via its own standalone top-level menu item, never from the Mega Menu tile bearing its name. Fixed by repointing the tile (now merged with the old separate "Shipments" tile, since Dispatch Board covers both) to the real OWL action. Mission Control's "Unassigned shipments" attention-strip pill had the identical class of issue (deep-linking to the stock shipment list) and was repointed too.

Verified: XML well-formed, JS syntax via `node --check`, full `web.assets_backend` SCSS bundle compiled via libsass (34 files, individually and in manifest order), ruff/pylint clean on `deployfleet_ui`, `deployfleet_dispatch_compliance`, `deployfleet_route`, `deployfleet_customer`, and `deployfleet_leave`, all touched ACL CSVs parse correctly, two new regression tests for the driver-availability scoring fix. **Not yet verified in a live browser** — same standing caveat as every screen in this module, and now the largest single batch to carry it (five workspaces at once).

**This closes the Dispatch & Trips domain in full — the third domain to reach complete status under doc 20's domain-completeness-first rollout**, alongside Fleet & Vehicles and Driver & HR. Doc 20 §3/§8 updated accordingly, including retroactively resolving two stale open questions (Maintenance domain split, Customers domain carve-out) that the user had already answered earlier in the session but the doc hadn't caught up to yet.

**Still genuinely open, not blocking:** Driver & HR's one remaining gap (a fleet-wide Driver Performance events ledger); redeploying and click-testing everything built since the last confirmed-live deploy (all of Fleet & Vehicles' ten workspaces, Driver & HR's four, and this domain's five) on the demo server — nothing built since early in the Experience Architecture work has been verified in an actual browser; and the domain build order for what's left (Compliance, Billing & Finance, AI & Intelligence) — no order confirmed yet, ask before starting the next one.
